CORE

IGAME + CORE + IGRAPHIC
                   ||
                 getDrawLoader()
                   ||
                 IDrawLoader()

                   ISHAPE ==> convert(IDrawLoader)
                                   IDrawLoader[RECT].load(*this)
